options(menu.graphics = FALSE)
options(repos=structure(c(CRAN="http://mirrors.dotsrc.org/cran/")))
#options(repos=structure(c(CRAN="http://cran.uni-muenster.de/")))

if(interactive()){
  library(colorout)
  library(setwidth)
  options(vimcom.verbose = 1)
  library(vimcom)
}
